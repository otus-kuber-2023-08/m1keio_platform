apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ name }}-pvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: {{ storage_size }}
