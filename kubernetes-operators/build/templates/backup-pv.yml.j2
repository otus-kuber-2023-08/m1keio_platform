apiVersion: v1
kind: PersistentVolume
metadata:
  name: backup-{{ name }}-pv
spec:
  persistentVolumeReclaimPolicy: Retain
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: {{ storage_size }}
  hostPath:
    path: /data/backup-{{ name }}-pv/
